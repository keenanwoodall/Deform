namespace Deform
{
	/// <summary>
	/// Holds different modes and spaces for noise to be applied in.
	/// </summary>
	public enum NoiseMode : byte
	{
		_3D,
		Directional,
		Normal,
		Spherical,
		Color
	}
}